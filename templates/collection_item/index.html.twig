{% extends 'layout/page-container.html.twig' %}

{% block title %}Items{% endblock %}

{% block content %}
    <div class="foreground">
        <h1 class="m-0 text-center">Items</h1>
    </div>

    <div class="foreground">
        <div class="list-group">
            {% for item in items %}
                {% include 'collection_item/item.html.twig' %}
            {% endfor %}
        </div>
    </div>
{% endblock %}

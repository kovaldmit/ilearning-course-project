<a href="{{ path('collection_container_show', {'id': container.id}) }}"
   class="list-group-item list-group-item-action d-flex flex-wrap justify-content-between align-items-center">
    <div>
        <h5 class="mb-1">{{ container.name }}</h5>
        <p class="mb-1">{{ container.getUnformattedDescription }}</p>
    </div>
    <div class="d-flex flex-wrap">
        <div class="p-3">
            <p>{{ container.collectionCategory }}</p>
        </div>
        <div class="p-3">
            <p>{{ container.user }}</p>
        </div>
    </div>
</a>
<footer>
    <div class="first">
        <div class="container">
            <div class="row">
                <div class="col-4">

                </div>
                <div class="col-4 d-flex align-items-center justify-content-center">
                    <div class="copyright">
                        <a href="{{ url('home') }}">ilearn</a> &copy; 2024
                    </div>
                </div>
                <div class="col-4">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <div class="support-request">
                            <div class="my-1">
                                <a href="{{ path('ticket_index') }}"
                                   class="link-underline link-underline-opacity-0 link-underline-opacity-75-hover">Support
                                    Requests</a>
                            </div>

                            <div class="my-1">
                                <a href="{{ path('ticket_new') }}"
                                   class="link-underline link-underline-opacity-0 link-underline-opacity-75-hover">Create
                                    Request</a>
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</footer>
